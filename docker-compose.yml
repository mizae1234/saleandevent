version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: saran-jeans-web
    network_mode: host
    environment:
      - NODE_ENV=production
      - PORT=3003
    env_file:
      - .env
    restart: unless-stopped
